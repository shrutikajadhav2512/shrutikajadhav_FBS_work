# convert the time entered in hh,min and sec into sethods
# take time
hour=float(input('Enter hour:'))
minute=float(input('Enter minutes:'))
# calculations
h=hour*3600
m=minute*60
# display
print(f'hour is:{h} seconds,minute is:{m} seconds:')
